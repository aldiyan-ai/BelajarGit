using UnityEngine;

public class TambahNamaDoni : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Nama Saya yan");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
